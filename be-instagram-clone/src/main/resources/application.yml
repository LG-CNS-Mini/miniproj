server:
    port: 8088
spring:
    application:
        name: be-instagram-clone
    #maria db
    datasource:
        url: ${DB_URL}
        username: ${DB_USER}
        password: ${DB_PASSWD}
        driver-class-name: ${DB_DRIVER}
    #h2 db
    #  datasource:
    #    driver-class-name: org.h2.Driver
    #    #url: jdbc:h2:mem:inspire
    #    url: jdbc:h2:file:./data/inspire;AUTO_SERVER=TRUE
    #    username: root
    #    passwd:
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                format_sql: true
                dialect: org.hibernate.dialect.MariaDBDialect

jwt:
    secret: ${JWT_SECRET_KEY}

file:
  upload-dir: uploads